name: nravepay
description: Accepting payment using Flutterwave in flutter.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  nwidgets:
    path: ../nwidgets
  cupertino_icons: ^1.0.2
  dio:
  equatable:
  flutter_svg:
  tripledes:
  intl:
  get_it:
  webview_flutter:
  

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/
